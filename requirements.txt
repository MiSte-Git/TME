cairosvg>=2.7
Pillow>=10.0
PySide6>=6.5
PyYAML>=6.0
keyring>=25.0
lottie>=0.6.11
odfpy>=1.4.1
telethon>=1.34

# cairosvg: bisher nur indirekt vorausgesetzt (dev-seitig zum Rastern der
# Flaggen-Icons genutzt, siehe Tools & Ressourcen in TME-Backlog.md), aber
# nirgends deklariert. Jetzt zusätzlich zur Laufzeit nötig: PNG-Rendering
# von TGS/Lottie-Custom-Emojis läuft seit dem Fix für TME-Backlog.md Punkt 1
# über lottie.exporters.cairo, das ohne cairosvg gar nicht definiert ist
# (siehe pipeline/frame_compositing.py::render_tgs_multiframe). cairosvg
# hängt nativ von libcairo ab - unter Windows ist das PyInstaller-Bundling
# davon bisher NICHT durch einen echten Build/Install/Export-Zyklus
# verifiziert (siehe Docstring von render_tgs_multiframe).

# Automatische Transkription von Sprachnachrichten (optional, ~4-5 GB inkl.
# CUDA-Deps): pip install -r requirements-stt.txt. Ohne diese Installation
# erscheinen Sprachnachrichten im ODT einfach ohne Transkript, kein Fehler.
